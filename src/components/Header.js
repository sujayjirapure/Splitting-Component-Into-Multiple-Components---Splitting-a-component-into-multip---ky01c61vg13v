import React from "react"

function Header(){
    return (
        <div id="header">
            <header id="my-header">
                This is the header
            </header>
        </div>
    );
}

export default Header;
